---
title: deepseek-harness 集成（dsh 桥）
description: 让桌宠实时感知 dsh 任务状态，以气泡 + 语音播报
---

# deepseek-harness 集成（dsh 桥）

桌宠实时感知 [deepseek-harness](https://github.com/deepseek-ai/deepseek-harness)（dsh）任务状态：任务开始 / 完成 / 失败 / 中断时，Live2D 角色以文字气泡 + 语音播报。

## 安装

接入只需在 dsh 侧装一个插件：

```bash
dsh plugin --profile web add @zapmomo-ai/dsh-plugin
```

装完重启 `dsh web` 即生效。ZapMomo 设置页「外部感知 · dsh 桥」默认已启用，无需额外配置。

## 验证

设置页点「测试播报」，桌宠弹气泡并播报即链路通；之后 dsh 任务开始 / 结束时桌宠会自动播报。

## 配置（可选）

设置页可开关语音播报、是否写入对话记录；也可编辑 `~/.zapmomo/settings.toml` 的 `[dsh]` 段：

| 配置项 | 缺省 | 说明 |
| --- | --- | --- |
| `enabled` | `true` | 是否启用桥服务 |
| `port` | `0` | 监听端口，`0` = 随机 |
| `voice_enabled` | `true` | 事件是否语音播报（语音会话中只出气泡） |
| `record_to_history` | `true` | 事件是否写入对话记录 |

## 更多

事件映射、安全与隔离、插件开发与发布见 [dsh-plugin README](../../../../integrations/dsh-plugin/README.md)。
