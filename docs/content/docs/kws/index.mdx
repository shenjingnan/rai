---
title: 关键词唤醒词（KWS）
description: 接入 sherpa-onnx 关键词检测模型，实现「说出唤醒词 → 程序反应」
---

# 关键词唤醒词（KWS）

接入 sherpa-onnx 关键词检测模型（zipformer 中英混合），实现「说出唤醒词 → 程序反应」。

通过 [KWS 快速开始](quick-start) 可以在几分钟内跑通：下载模型 → 离线验证 → 实时监听。
